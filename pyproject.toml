[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "litho-capacity-ai"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["torch", "numpy", "pandas"]

[tool.setuptools.packages.find]
where = ["src"]
